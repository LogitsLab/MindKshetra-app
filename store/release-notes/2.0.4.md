# What's new in 2.0.4

Fixes a crash after signing in during onboarding, and stops the black-screen flicker on restart for signed-in accounts.

Also improves Android layout: the home brand title scales to the screen, and the tab bar clears the system navigation bar.
