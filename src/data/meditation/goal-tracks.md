# Goal tracks (Tier 3) — deferred

See web docs: MindKshetra/docs/goal-tracks.md

Content stubs will live here as `tracks/<track>-7.json` when stewarded.
Recorded audio uses `audio_url` on each session; TTS remains the fallback.
